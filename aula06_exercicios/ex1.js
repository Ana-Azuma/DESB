// Cria um array com 5 elementos
const numeros = [1, 2, 3, 4, 5];

// Exibe o array antes da alteração
console.log("Antes da alteração:", numeros);

// Altera o valor do primeiro elemento (índice 0)
numeros[0] = 99;

// Exibe o array depois da alteração
console.log("Depois da alteração:", numeros);

